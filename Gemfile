source 'https://rubygems.org'


# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.1.5'
# Use postgresql as the database for Active Record
gem 'pg'
# Use SCSS for stylesheets
#gem 'sass-rails', '~> 4.0.3'
# Use Uglifier as compressor for JavaScript assets
#gem 'uglifier', '>= 1.3.0'
# bundle exec rake doc:rails generates the API under doc/api.
#gem 'sdoc', '~> 0.4.0',        group: :doc

# Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
gem 'spring',      group: :development

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]

# DB
gem 'foreigner' #foreign keys

# JSON Responses
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
gem 'yajl-ruby' # To increase speed of JSON generation
gem 'rack-json-api-version', require: 'rack/json/api/version' # api version in header
gem 'rack-cors', require: 'rack/cors' # cross AJAX

# Uploads
gem 'paperclip'
gem 'aws-sdk'
gem 'mini_magick' # like ImageMagick

# WEB SERVICES
gem 'rest-client'

# ACTIVERECORD
gem 'acts_as_list'
gem 'auto_strip_attributes', '~> 2.0'
gem 'state_machine'
gem 'profanity_filter'

# AUTHORIZATION
gem 'omniauth'
gem 'omniauth-facebook'
gem 'devise'

# YAML
# Prior to 0.1.6, libyaml is vulnerable to a heap overflow exploit from malicious YAML payloads.
# https://www.ruby-lang.org/en/news/2014/03/29/heap-overflow-in-yaml-uri-escape-parsing-cve-2014-2525/
gem 'psych', '~> 2.0.5'

# Use unicorn as the app server
gem 'unicorn'

# Facebook REST
gem "koala"

#Redis
gem "redis"

# Use httparty gem to simplify http requests
gem 'httparty'
